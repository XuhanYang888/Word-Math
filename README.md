# Word-Math

An interactive web app that uses static word embedding matrices (GloVe 6B) to present words as points in a high-dimensional  vector space. You can write equations using everyday words and the engine evaluates it to find and display the nearest text coordinates. It also projects the those words  onto an clickable 3D map.
